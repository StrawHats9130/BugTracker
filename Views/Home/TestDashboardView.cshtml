
@{
    ViewBag.Title = "TestDashboardView";
}

<h2>TestDashboardView</h2>

<h2 data-toggle="modal" data-target="#inviteMember" class="btn">HouseHoldDashboard</h2>
@if (User.IsInRole("BigChief"))
{
    <div class="row">

        <div class="col">
            @*@Html.ActionLink("invite Member", "Join", "Households", new { @class = "btn btn-outline-success" })*@

            <button data-toggle="modal" data-target="#inviteMember" class="btn btn-outline-warning">Invite New Member</button>
        </div>
    </div>

}

<div class="row">

    <div class="col">
        @*@Html.ActionLink("invite Member", "Join", "Households", new { @class = "btn btn-outline-success" })*@

        @*<button data-toggle="modal" data-target="#inviteMember1" class="btn btn-outline-warning">Invite New Member</button>*@
    </div>
</div>
<div>

</div>

<!-- Modal-->
<!-- The Modal -->
<div class="modal" id="inviteMember">
    <div class="modal-dialog">
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="modal-header">
                <h4 class="modal-title">Create an inventation to the HouseHold</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <!-- Modal body -->
            <div class="modal-body">

                @using (Html.BeginForm("CreateAsync", "Invitations"))
                {
                    @Html.AntiForgeryToken()

                    <div class="form-group">
                        @Html.Label("Recipient", htmlAttributes: new { @class = "control-label col-md-2" })
                        <div class="col-md-10">
                            @Html.Editor("RecipientEmail", new { htmlAttributes = new { @class = "form-control" } })
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.Label("Subject", htmlAttributes: new { @class = "control-label col-md-2" })
                        <div class="col-md-10">
                            @Html.Editor("Subject", new { htmlAttributes = new { @class = "form-control" } })
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.Label("Body", htmlAttributes: new { @class = "control-label col-md-2" })
                        <div class="col-md-10">
                            @Html.Editor("Body", new { htmlAttributes = new { @class = "form-control" } })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Invite" class="btn btn-default" />
                        </div>
                    </div>

                }


                Modal body..
            </div>

            <!-- Modal footer -->
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>

        </div>
    </div>
</div>